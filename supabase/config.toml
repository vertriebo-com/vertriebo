# Supabase CLI config for Vertriebo
# This file intentionally contains NO secrets.
# Keep SUPABASE_URL and SUPABASE_SERVICE_KEY only in Base44/Supabase secrets.

project_id = "vertriebo"

[api]
enabled = true

[db]
# Local development default port. Remote project is configured through Supabase CLI/env.
port = 54322

[functions]
# Edge Functions may be added later. Current Phase 1 uses Base44 backend functions + Supabase REST.
